# Development database seed

Run `npm run seed` with a development environment to create the demo dataset.

## Development accounts

| Account type | Email                  | Password         |
| ------------ | ---------------------- | ---------------- |
| TENANT       | owner@easystore.lat    | EasyStoreDev123! |
| CUSTOMER     | customer@easystore.lat | EasyStoreDev123! |
| EMPLOYEE     | manager@easystore.lat  | EasyStoreDev123! |

These credentials are for local development only. The seed stores a bcrypt hash and does not write passwords or hashes to runtime logs.
